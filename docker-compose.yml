services:
  mysql:
    image: ${MYSQL_IMAGE:-registry.cn-hangzhou.aliyuncs.com/library/mysql:8.0}
    container_name: personal-resume-mysql
    command:
      - --character-set-server=utf8mb4
      - --collation-server=utf8mb4_unicode_ci
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-1006}
      MYSQL_DATABASE: personal_resume
    ports:
      - "13306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
      - ./database/schema.sql:/docker-entrypoint-initdb.d/01-schema.sql:ro
      - ./database/seed.sql:/docker-entrypoint-initdb.d/02-seed.sql:ro
      - ./database/fix_profile_is_active.sql:/docker-entrypoint-initdb.d/03-fix-profile-is-active.sql:ro
      - ./database/update_profile_asset_urls.sql:/docker-entrypoint-initdb.d/04-update-profile-asset-urls.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h 127.0.0.1 -uroot -p$${MYSQL_ROOT_PASSWORD} --silent"]
      interval: 5s
      timeout: 5s
      retries: 20
    restart: unless-stopped

  backend:
    build:
      context: ./backend
      args:
        MAVEN_IMAGE: ${MAVEN_IMAGE:-registry.cn-hangzhou.aliyuncs.com/library/maven:3.9.9-eclipse-temurin-17}
        JRE_IMAGE: ${JRE_IMAGE:-registry.cn-hangzhou.aliyuncs.com/library/eclipse-temurin:17-jre}
    container_name: personal-resume-backend
    environment:
      DB_URL: jdbc:mysql://mysql:3306/personal_resume?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
      DB_USERNAME: root
      DB_PASSWORD: ${MYSQL_ROOT_PASSWORD:-1006}
      DB_DDL_AUTO: none
      APP_JWT_SECRET: ${APP_JWT_SECRET:-this-is-a-local-dev-secret-at-least-32-characters}
      APP_CORS_ALLOWED_ORIGINS: ${APP_CORS_ALLOWED_ORIGINS:-*}
    expose:
      - "8080"
    depends_on:
      mysql:
        condition: service_healthy
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      args:
        NODE_IMAGE: ${NODE_IMAGE:-registry.cn-hangzhou.aliyuncs.com/library/node:20-alpine}
        NGINX_IMAGE: ${NGINX_IMAGE:-registry.cn-hangzhou.aliyuncs.com/library/nginx:1.27-alpine}
        NPM_REGISTRY: ${NPM_REGISTRY:-https://registry.npmmirror.com}
    container_name: personal-resume-frontend
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: unless-stopped

volumes:
  mysql_data:
